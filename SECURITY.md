# Security policy

CrowPanel Pocket Arcade is an offline embedded firmware project. It does not initialize
Wi-Fi or Bluetooth, does not accept network connections and does not process external
files at runtime.

If you find a security-relevant issue in the source code or build configuration, open a
GitHub issue with enough information to reproduce the problem. Avoid publishing secrets,
private device data or credentials in issue reports.
